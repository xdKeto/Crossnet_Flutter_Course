import 'package:flutter/material.dart';

class About extends StatelessWidget {
  const About({super.key});

  // types, species, weight
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}