import 'package:flutter/material.dart';

class Coba extends StatelessWidget {
  final String nama;
  const Coba({super.key, required this.nama});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}